# Don't commit full-size images to repo (Hugo processes these locally)
content/gallery/*.jpg
content/gallery/*.JPG
content/gallery/*.jpeg
content/gallery/*.png
content/gallery/Piclist.txt

# Hugo build cache
.hugo_build.lock

# Hugo resources cache (regenerated locally)
resources/

# Local scripts (tools folder stays local only)
tools/